{{- if .Values.authguard.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "authguard.serviceAccountName" . }}
  labels:
    {{- include "authguard.labels" . | nindent 4 }}
  {{- with .Values.authguard.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.secrets.serviceAccount.annotations }}
  {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false
{{- end }}
