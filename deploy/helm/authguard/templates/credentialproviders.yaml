{{- $provider := include "authguard.secretsProvider" . -}}
{{- if eq $provider "gcp" }}
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ include "authguard.fullname" . }}-gcp-secrets
  labels:
    {{- include "authguard.labels" . | nindent 4 }}
    app.kubernetes.io/component: credential-provider
spec:
  provider: gcp
  parameters:
    secrets: |
      - resourceName: {{ required "secrets.gcp.secretResourceName is required when secrets.provider=gcp" .Values.secrets.gcp.secretResourceName | quote }}
        fileName: env
{{- else if eq $provider "aws" }}
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ include "authguard.fullname" . }}-aws-secrets
  labels:
    {{- include "authguard.labels" . | nindent 4 }}
    app.kubernetes.io/component: credential-provider
spec:
  provider: aws
  parameters:
    region: {{ required "secrets.aws.region is required when secrets.provider=aws" .Values.secrets.aws.region | quote }}
    objects: |
      - objectName: {{ required "secrets.aws.secretName is required when secrets.provider=aws" .Values.secrets.aws.secretName | quote }}
        objectType: "secretsmanager"
        objectAlias: env
{{- else if not (has $provider (list "kubernetes" "vault")) }}
{{- fail "secrets.provider must be one of kubernetes | gcp | aws | vault" }}
{{- end }}
