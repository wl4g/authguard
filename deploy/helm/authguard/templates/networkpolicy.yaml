{{- $networkPolicy := .Values.authguard.networkPolicy -}}
{{- if $networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "authguard.fullname" . }}
  labels:
    {{- include "authguard.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "authguard.serverSelectorLabels" . | nindent 6 }}
  policyTypes: [Ingress]
  ingress:
    - from:
        - {{- with $networkPolicy.envoyProxyNamespaceSelector }}
          namespaceSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          podSelector:
            matchLabels:
              {{- toYaml $networkPolicy.envoyProxyPodSelector | nindent 14 }}
      ports:
        - { protocol: TCP, port: {{ .Values.authguard.service.grpcPort }} }
    - from:
        - {{- with $networkPolicy.workloadNamespaceSelector }}
          namespaceSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          podSelector:
            matchLabels:
              {{- toYaml $networkPolicy.workloadPodSelector | nindent 14 }}
      ports:
        - { protocol: TCP, port: {{ .Values.authguard.service.scopeGrpcPort }} }
    {{- if .Values.authguard.mgmt.enabled }}
    - from:
        - {{- with $networkPolicy.managementNamespaceSelector }}
          namespaceSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          podSelector:
            matchLabels:
              {{- toYaml $networkPolicy.managementPodSelector | nindent 14 }}
      ports:
        - { protocol: TCP, port: {{ .Values.authguard.service.mgmtPort }} }
    {{- end }}
{{- end }}
